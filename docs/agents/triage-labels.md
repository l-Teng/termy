# Triage labels

The engineering skills use these canonical roles and tracker labels:

- `needs-triage` maps to `needs-triage`
- `needs-info` maps to `needs-info`
- `ready-for-agent` maps to `ready-for-agent`
- `ready-for-human` maps to `ready-for-human`
- `wontfix` maps to `wontfix`

When a skill mentions a triage role, use the corresponding tracker label.
