# Repository Coverage Ledger

Closed repository-wide frontier. Every row is reconciled to discovery-time evidence in `artifacts/02_discovery/work_ledger.jsonl` and, for `reportable` rows, to one or more matching objects in `artifacts/02_discovery/raw_candidates.jsonl`. Counts: 17 reportable, 9 suppressed, 0 not applicable, 0 deferred.

| Row | Boundary / shard | Files or controls to check | High-impact family | Source / privileged boundary | Sink / control | Candidate IDs | Disposition | Evidence / closure |
|---|---|---|---|---|---|---|---|---|
| COV-API-AUTH | Hosted API authentication and sessions | `crates/api/src/main.rs`, auth router and framework configuration | auth bypass, session misbinding, unsafe security configuration | unauthenticated HTTP request | better-auth configuration and `/auth` handlers | `shard-aab-cand-1`, `shard-aab-cand-2` | reportable | `main.rs` binds the combined router publicly, enables password sign-in, and mounts BetterAuth under `/auth`. The lock-resolved adapter buffers with `usize::MAX` before its nominal body limit and derives rate-limit identity from unverified forwarding headers. |
| COV-API-AUTHZ | Hosted API application routes | `crates/api/src/routes.rs` and router composition | missing auth, BOLA/IDOR, protected data exposure | unauthenticated or authenticated HTTP client | route-local session extraction and object access |  | suppressed | Exact route enumeration found only public `/health`, protected `/api/me`, and the framework `/auth` mount. `/api/me` requires `CurrentSession`, whose reviewed extractor validates token existence, session activity/expiry, and loads only the session-bound user; no caller-selected object identifier exists. |
| COV-API-QUERY | Hosted API database access | `crates/api/src/db.rs`, routes and migrations | SQL injection, database transport boundary | HTTP/auth state and operator database URL | SQL query construction and connection selection |  | suppressed | The application query surface is the fixed parameter-free `SELECT 1`; BetterAuth's reviewed database adapter uses parameter binding. `db.rs`'s substring transport selection and plaintext direct branch are operator deployment assumptions with no current sensitive application rows or request-derived SQL. |
| COV-PLUGIN-INSTALL | Local/GitHub plugin installation | plugin runtime discovery/install paths and desktop plugin settings | pre-load code execution, path traversal, arbitrary file write | remote repository or local plugin source | download, copy, managed-root and source validation |  | suppressed | `crates/cli/src/commands/plugins.rs`, desktop plugin settings, and `plugin_runtime/src/lib.rs` close both flows: immutable GitHub commit resolution, truncated-tree/symlink/submodule rejection, count/byte limits, normalized destinations, explicit trusted-code warning/confirmation, randomized replacement, and no code or package hooks before load. |
| COV-PLUGIN-IMPORT | Plugin import and bundling | `crates/plugin_runtime` source resolution and bundling | path traversal, symlink escape, dynamic import/code execution | trusted-but-not-yet-loaded plugin tree | import allowlist, root containment, Bun build/load |  | suppressed | `worker.ts` re-captures and hashes the bounded symlink-free snapshot immediately before load, confines relative imports to captured files, rejects absolute/package imports, and performs no package installation. Loaded Bun/Node authority begins only at the explicitly trusted load phase. |
| COV-PLUGIN-PROTOCOL | Persistent Bun host and Worker protocol | `crates/plugin_runtime/src/host.ts`, `worker.ts`, Rust protocol code | protocol injection, unsafe parsing, application-wide DoS | plugin output and Worker messages | JSON framing, descriptor/action/document validation, timeouts and limits | `shard-abo-cand-1` | reportable | Framing, request/response size, queue, timeout, catalog, action, storage, and native-view shapes are bounded and validated. A distinct raw candidate remains in Rust settings resolution: dot-delimited plugin/setting IDs create ambiguous keyring accounts, allowing a loaded plugin to receive or delete another plugin's secret through the normal protocol context. |
| COV-UPDATER-FETCH | Release discovery and update download | `crates/release_core`, `crates/auto_update` | SSRF, redirect abuse, artifact substitution | GitHub release metadata and network response | repository URL, asset URL selection, redirect and download sink | `shard-aac-cand-1`, `shard-abp-cand-1`, `shard-aci-cand-1` | reportable | Rust fixes the repository and uses GitHub HTTPS, but installer and checksum URLs come from the same release and provide no independent publisher identity. The Swift updater separately auto-parses official `tag_name` at launch and force-unwraps numeric components after a broader Unicode/overflow guard. |
| COV-UPDATER-INSTALL | Platform installer handoff | updater install functions and platform scripts | command injection, path traversal, arbitrary file overwrite, privilege escalation | downloaded artifact and mounted/archive contents | checksum policy, mount/extract, process argv, replacement path | `shard-aac-cand-1`, `shard-aac-cand-2`, `shard-abp-cand-1` | reportable | macOS copies an app from the downloaded DMG and Windows uses `ShellExecuteW(..., runas)` without verifying an independent publisher identity. Windows also hashes a predictable user-writable pathname, drops that file handle, and later reopens the path across an async boundary for elevation. |
| COV-SSH-ARGV | Saved SSH host launch | `crates/ssh_core`, desktop SSH adapters | command injection and unsafe path handling | saved/imported hostname, username, port and identity path | OpenSSH program-plus-argv construction | `shard-abr-cand-1` | reportable | Host fields are length/control/option-prefix validated and passed to fixed OpenSSH as separate argv with `--` before hostname; no shell injection was found. In key mode, however, the constructed argv leaves password/keyboard-interactive fallback enabled while routing all prompts to the stored key-passphrase secret, matching the malicious-server candidate. |
| COV-SSH-ASKPASS | SSH credential helper | `crates/ssh_core` askpass and desktop helper entrypoints | credential disclosure, confused deputy, host-key bypass | OpenSSH prompts and local process environment | parent/process validation, keychain lookup, prompt allowlist | `shard-aaw-cand-1`, `shard-abr-cand-1` | reportable | UUID/kind/prompt syntax and parent lineage are checked, but name-plus-grandparent PID can be impersonated by a direct Termy child named `ssh`. Separately, generic password/passphrase prompt matching cannot distinguish local private-key decryption from a server-controlled keyboard-interactive challenge. |
| COV-TERMINAL-OSC | Terminal escape and OSC parsing | `crates/core`, `crates/terminal_ui`, desktop link/clipboard handlers | command injection, unsafe URL/file activation, parser confusion | remote/local terminal bytes | OSC parser, interceptors, link allowlist and UI actions | `shard-aan-cand-1`, `shard-aao-cand-1`, `shard-aao-cand-2`, `shard-abd-cand-1`, `shard-abk-cand-1`, `shard-acg-cand-1` | reportable | The interceptor has a 64 KiB sequence cap and link activation uses explicit gestures/typed openers, but raw candidates cover Windows UNC resolution from OSC working-directory/title metadata, unauthenticated OSC 52 clipboard read/write, a UTF-8 byte-slice panic in OSC 8 hover preview, and background Swift-tab clipboard overwrite. |
| COV-TERMINAL-KITTY | Kitty graphics and binary payloads | core protocol/graphics and rendering adapters | unsafe parsing, memory exhaustion, image-decoder attack surface | terminal byte stream | image decode, placement limits, allocation and rendering | `shard-aam-cand-1`, `shard-aam-cand-2` | reportable | Image bytes, decompression, dimensions, decoded PNG storage, and total image bytes are bounded. The same parser still accepts remote `t=f`/`t=t` local-file paths (including constrained temp deletion) and appends unkeyed persistent placements without a count quota, amplifying heap and render work. |
| COV-TMUX-PARSER | tmux control-mode parser | `crates/tmux_control_core`, terminal/desktop tmux adapters | protocol injection, parser desync, command-response confusion, DoS | tmux control bytes and remote session state | framing, token matching, notification parsing and bounds | `shard-abe-cand-1`, `shard-abt-cand-1`, `shard-abv-cand-1`, `shard-abz-cand-1`, `shard-aca-cand-1`, `shard-aca-cand-2` | reportable | Syntax/tag checks and bounded channels/coalesced pane-output queues exist, but raw candidates cover unbounded pre-newline framing, per-block and cross-block command-response accumulation that survives caller timeout, unbounded snapshot pane fan-out with serial capture timeouts, and unchecked two-dimensional pane allocation. |
| COV-TMUX-COMMAND | tmux command construction | tmux command helpers and desktop actions | command injection | session/window/pane names and user actions | tmux command quoting and control-channel write |  | suppressed | `tmux_control_core/src/command.rs` single-quotes general arguments with embedded-quote escaping, rejects CR/LF/NUL in control arguments, and hex-encodes terminal input. Desktop/terminal UI actions resolve typed current snapshot identities and pass rename/split/move/kill/switch/layout values as arguments, not shell text. |
| COV-CONFIG-PATH | Configuration and theme paths | `crates/config_core`, theme stores and desktop config IO | path traversal, arbitrary file read/write/delete | user-writable config, imported themes and paths | resolve/join/canonicalize/atomic persistence controls | `shard-aaf-cand-1`, `shard-aaf-cand-2`, `shard-abl-cand-1`, `shard-abv-cand-2`, `shard-acg-cand-2` | reportable | Normal config persistence uses a fixed per-user path, typed parsing, normalized theme IDs, and atomic replacement. Raw candidates remain for theme-repository metadata reads and symlink-following exports outside `--repo`, plus GPUI and Swift theme installers accepting registry-controlled absolute HTTP(S) URLs for local/private-network GETs. |
| COV-WORKSPACE-PERSIST | Workspace and buffer persistence | desktop and Swift persistence modules | unsafe deserialization, path traversal, data loss/DoS | user-writable persisted JSON/buffer state | parse, allocation, file selection and atomic replacement | `shard-acf-cand-1` | reportable | GPUI SQLite uses bound queries and checked/clamped restore state; Swift writes the fixed workspace file atomically with owner-only permissions and version checks. Swift restore nevertheless reads unbounded JSON and eagerly expands unbounded pane/buffer/layout state into recursive views and terminal processes. |
| COV-DEEPLINK | Desktop deep links and custom schemes | GPUI and Swift deep-link handlers | command/action injection, open redirect, unsafe file/host selection | external URL/scheme invocation | parser, command allowlist and action dispatch | `shard-aap-cand-1`, `shard-abl-cand-1` | reportable | Both handlers accept only closed `termy` routes; external command text is discarded and theme installation uses normalized slugs plus confirmation. GPUI `new?dir=` still accepts Windows UNC authorities that are probed automatically, and confirmed theme deep links reuse the registry-controlled absolute asset URL policy. |
| COV-BROWSER | Embedded browser tabs and external links | desktop browser/link modules | web-to-native privilege escape, unsafe scheme/file access | untrusted web content and terminal hyperlinks | navigation policy, scheme allowlist and native dispatch | `shard-aaw-cand-2`, `shard-aaw-cand-3` | reportable | Browser navigation blocks file/javascript/arbitrary app schemes, and Swift terminal links allow only HTTP(S), FTP, and mailto after Command-click. GPUI browser raw candidates remain for subframe-driven native address-bar spoofing and unbounded recognized IPC messages feeding uncapped native toast queues. |
| COV-FILE-DROP | Desktop file drops and open helpers | GPUI and Swift file-drop/context-menu paths | path injection, shell/command injection | dragged files and terminal output paths | quoting, URI conversion and paste/open behavior |  | suppressed | GPUI and Swift accept explicit local file drags, reject non-file URLs/control characters, and paste single-quoted paths with embedded apostrophes escaped. Image drops use random temporary filenames and do not decode or execute content; native menu/open actions remain explicit typed gestures. |
| COV-FFI | C-compatible embedding surface | `crates/ffi/src/lib.rs`, header and native bridge | memory safety, panic escape, ownership/lifetime errors | documented C caller and terminal bytes | pointer/length validation, catch_unwind, allocation/free contract |  | suppressed | Full header/Rust/Swift comparison found aligned ABI shapes, required null/UTF-8 checks, `catch_unwind` guards, matching owned-buffer free/reset APIs, handle teardown after wake-thread join, and caller-serialized production use. No compliant documented call produced memory unsafety, panic escape, or lifetime/ownership mismatch. |
| COV-WEBSITE-CONTENT | Public website content and routes | `website/src/routes`, MDX/markdown rendering | XSS, path traversal, open redirect | HTTP path/query and release/content metadata | content lookup, markdown renderer, links and response headers |  | suppressed | Every docs, Markdown, release, download, search, and text route was reviewed. Slugs address a generated in-memory collection, release tags are encoded, JSX escapes text, raw Markdown HTML is dropped, React blocks `javascript:` URLs, cache count is bounded, and no request value forms a filesystem path or redirect. |
| COV-WEBSITE-FETCH | Website server-side network calls | website GitHub release/sponsor helpers | SSRF and cache poisoning | request context and remote GitHub data | outbound destination construction and fetch |  | suppressed | All server fetches are anchored to fixed `https://api.github.com` and the configured repository; caller-supplied tags pass through `encodeURIComponent`, sponsor URLs are compile-time data and browser-only, failures close generically, and no request field can change scheme, host, credentials, or repository. |
| COV-RELEASE-CI | Release CI and packaging | `.github/workflows`, `scripts`, installer definitions | supply-chain compromise, secret exposure, command injection | contributor changes, tags, workflow inputs and dependencies | secret-bearing jobs, action pinning, shell interpolation and artifact upload | `shard-adc-cand-1` | reportable | Release/build scripts otherwise use closed platform/architecture mappings, quoted argv, explicit credential completeness, signature/notary checks, fixed staging paths, and pinned/checksummed AppImage tooling. The DMG readiness gate inconsistently counts real app directories but follows `Termy.app` symlinks, allowing validation of an external app before candidate upload. |
| COV-CLI | CLI update, plugin, theme and config commands | `crates/cli` | command injection, path traversal, arbitrary file write | local CLI args and remote plugin/theme data | parsing, download/install, editor/process launch and managed paths | `shard-aae-cand-1`, `shard-aaf-cand-1`, `shard-aaf-cand-2`, `shard-aah-cand-1`, `shard-aaq-cand-1` | reportable | Clap/typed argv and managed plugin/theme/config controls prevent broad shell injection, but raw candidates cover CWD-relative executable fallbacks in CLI open and desktop delegation, out-of-root theme-repository reads/writes, and shell-profile mode broadening during CLI installation. |
| COV-INSTALLER | User-facing install scripts | `scripts/install-linux.sh`, PowerShell/Inno Setup and DMG scripts | download/command injection, unsafe archive handling, privilege escalation | network artifacts, environment and command-line args | URL selection, checksum/signature, extraction and install destinations | `shard-aah-cand-1`, `shard-add-cand-1`, `shard-adf-cand-1` | reportable | macOS builders validate fixed bundle members and use typed signing/notary argv. Unix CLI profile replacement loses original mode metadata; the AUR helper derives hashes from the same downloaded bytes while skipping PGP by default; and the Linux installer persists release executables without consuming the published checksum or any independent signature. |
| COV-DEPENDENCY | Reachable dependency and build policy | Cargo manifests/lock, Bun website lock, Docker and Nix | reachable vulnerable dependency, mutable supply chain | package registries, Git revisions and container bases | lock/pin policy and runtime-reachable vulnerable versions | `shard-aab-cand-1`, `shard-aab-cand-2` | reportable | Cargo/Bun locks are committed, Git dependencies are revision-pinned, website deployment uses frozen installs, and the reviewed Docker image is versioned. Cargo.lock nevertheless resolves runtime-reachable BetterAuth 0.8.0 whose Axum body buffering and forwarding-header rate-limit behavior are the two API raw candidates. |
