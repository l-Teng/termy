[package]
name = "tmon-ghostty-memory"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
alacritty_terminal = "0.26"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tmon_current = { package = "tmon", path = "../../crates/tmon" }

[build-dependencies]
cc = "1"
